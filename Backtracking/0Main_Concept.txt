Backtracking technique is about selecting a subset of options which matches a partiicular criteria.

1.How to consider options ? 
-> Recusion into the elements and done for each element.

2.When to select options as results ? 
-> When matches a particular criteria. Done by setting a base condition at the recursive function.